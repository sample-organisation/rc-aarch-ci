language: node_js

architecture: aarch64
os: Ubuntu_16.04

node_js:
  - 8.0.0

build:
  pre_ci_boot:
    image_name: drydockaarch64/u16
    image_tag: master

  ci:
    - node --version
    - uname -a
